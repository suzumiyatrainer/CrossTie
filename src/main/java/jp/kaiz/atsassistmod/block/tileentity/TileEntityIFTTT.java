package jp.kaiz.atsassistmod.block.tileentity;

import net.minecraft.tileentity.TileEntity;

/**
 * Compile-only stub class for ATSAssistMod compatibility.
 * This class is only used to satisfy the compiler when compiling Mixins.
 * It will not be packaged into the final release JAR, or even if it is,
 * the actual runtime class from ATSAssistMod will take precedence.
 */
public class TileEntityIFTTT extends TileEntity {
    public int getTick() {
        return 0;
    }
}
